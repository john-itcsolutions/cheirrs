version: 0.1
log:
  fields:
    service: registry
storage:
  cache:
    blobdescriptor: inmemory
  filesystem:
    rootdirectory: /home/postgres/registry
http:
  addr: 10.119.16.128:5000
  headers:
    X-Content-Type-Options: [nosniff]
auth:
  htpasswd:
    realm: basic-realm
    path: /home/postgres/.htpasswd
health:
  storagedriver:
    enabled: true
    interval: 10s
    threshold: 3
